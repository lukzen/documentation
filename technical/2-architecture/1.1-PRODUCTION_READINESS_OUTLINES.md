# Production Readiness Documentation - Master Outline

**Assessment Date:** 2025-11-28
**Primary Cloud Provider:** Alibaba Cloud (with AWS/GCP comparisons)

---

## Table of Contents

1. [Document Organization Strategy](#document-organization-strategy)
2. [Production Readiness: Agency App](#1-production_readiness_agency_appmd)
3. [Production Readiness: Backend Service](#2-production_readiness_backend_servicemd)
4. [Production Readiness: Backoffice App](#3-production_readiness_backoffice_appmd)
5. [Production Readiness: Alibaba Infrastructure](#4-production_readiness_alibaba_inframd)
6. [Code Quality Assessment](#5-code_quality_assessmentmd)
7. [Summary](#summary)

---

## Document Organization Strategy

This outline shows the structure of all 5 production readiness documents **after reorganization** to separate concerns:

- **Application Documents** (agency-app, backend-service, backoffice-app): Focus on **code quality, architecture, testing, and application-level improvements**
- **Infrastructure Document** (alibaba-infra): Focus on **deployment, Kubernetes, infrastructure automation, and operations**
- **Code Quality Document**: Focus on **cross-cutting code patterns and best practices**

---

## 1. PRODUCTION_READINESS_AGENCY_APP.md

### Purpose
Code-level production readiness assessment for customer-facing React SPA application.

### Outline

```
# Production Readiness Assessment: Agency App (Application Code)

## Executive Summary
- Application Overview
- Tech Stack (React 18.2, TypeScript 5.3, Vite 5.0, Mantine UI)
- Code Quality Score: X/100
- Key Code-Level Gaps

## Code Quality Metrics
| Category | Score | Focus Area |
|----------|-------|------------|
| Type Safety | X/100 | TypeScript strict mode, API types |
| Testing | X/100 | Component tests, integration tests |
| Error Handling | X/100 | Standardized error boundaries |
| Security (Code) | X/100 | Input validation, XSS prevention |
| Performance (Code) | X/100 | Code splitting, bundle optimization |
| Maintainability | X/100 | Component architecture, code organization |

## 1. TYPE SAFETY & API INTEGRATION

### 1.1 TypeScript Configuration
- Current: TypeScript 5.3 with strict mode
- Gaps: Missing strict null checks in some components
- Recommendations:
  - Enable `strictNullChecks`
  - Add `noUncheckedIndexedAccess`
  - Type all API responses with Zod

### 1.2 API Type Definitions
- Current: Some API calls lack proper typing
- Required: Generate types from OpenAPI schema
- Implementation:
  ```typescript
  // Code examples for type-safe API client
  ```

### 1.3 Form Validation
- Current: Basic Mantine form validation
- Required: Zod schema validation
- Implementation:
  ```typescript
  // Code examples for Zod-based forms
  ```

## 2. COMPONENT ARCHITECTURE & TESTING

### 2.1 Component Organization
- Current: Mixed container/presentational components
- Required: Clear separation of concerns
- Patterns:
  - Container components (logic)
  - Presentational components (UI)
  - Custom hooks for shared logic

### 2.2 Unit & Integration Testing
- Current Coverage: ~5%
- Required Coverage: >70%
- Test Strategy:
  - Component unit tests with @testing-library/react
  - Integration tests with MSW
  - Accessibility tests
- Examples:
  ```typescript
  // Component test examples
  // MSW handler examples
  ```

### 2.3 Error Boundaries
- Current: No error boundaries
- Required: Application-level error handling
- Implementation:
  ```typescript
  // Error boundary component
  // Error fallback UI
  ```

## 3. SECURITY (APPLICATION LEVEL)

### 3.1 Authentication Token Storage
- Current: localStorage (XSS vulnerable)
- Required: httpOnly cookies
- Migration Path:
  ```typescript
  // Cookie-based auth implementation
  ```

### 3.2 Input Validation & Sanitization
- Current: Basic validation
- Required: Comprehensive validation with Zod
- XSS Prevention:
  ```typescript
  // DOMPurify integration
  // Zod validation schemas
  ```

### 3.3 Content Security Policy (Code Changes)
- Required: Update build configuration
- Vite Configuration:
  ```typescript
  // vite.config.ts CSP headers
  ```

### 3.4 Environment Variable Validation
- Current: No validation
- Required: Runtime validation with Zod
- Implementation:
  ```typescript
  // env.ts with Zod schema
  ```

## 4. PERFORMANCE OPTIMIZATION (CODE LEVEL)

### 4.1 Code Splitting
- Current: Minimal code splitting
- Required: Route-based and component-based splitting
- Implementation:
  ```typescript
  // React.lazy() examples
  // Route-based splitting
  ```

### 4.2 Bundle Optimization
- Current: No bundle analysis
- Required: Analyze and optimize bundle size
- Tools:
  - rollup-plugin-visualizer
  - Remove unused dependencies
- Target: <500KB initial bundle

### 4.3 Performance Monitoring (Code Integration)
- Required: Add performance markers
- Implementation:
  ```typescript
  // Web Vitals tracking
  // Custom performance marks
  ```

### 4.4 Image Optimization
- Required: Lazy loading, responsive images
- Implementation:
  ```typescript
  // Lazy loading components
  // srcset implementation
  ```

## 5. STATE MANAGEMENT & DATA FETCHING

### 5.1 Redux Toolkit Optimization
- Current: Redux Toolkit 1.9.7
- Review: Proper RTK Query usage
- Best Practices:
  - Normalized state shape
  - Selector memoization
  - Proper typing

### 5.2 Caching Strategy
- Required: Implement proper cache invalidation
- RTK Query Configuration:
  ```typescript
  // Cache tag system
  // Invalidation strategies
  ```

## 6. LOGGING & ERROR REPORTING (APPLICATION SIDE)

### 6.1 Client-Side Logging
- Required: Structured logging
- Implementation:
  ```typescript
  // Logger utility
  // Log levels
  ```

### 6.2 Error Tracking Integration
- Required: Sentry or equivalent integration
- Configuration:
  ```typescript
  // Sentry.init() setup
  // Source map upload
  ```

## 7. ACCESSIBILITY & UX

### 7.1 WCAG Compliance
- Required: WCAG 2.1 AA compliance
- Focus Areas:
  - Keyboard navigation
  - Screen reader support
  - Color contrast
  - ARIA labels

### 7.2 Internationalization (i18n)
- Required: Prepare for multi-language support
- Implementation:
  ```typescript
  // i18next setup
  // Translation structure
  ```

## 8. IMPLEMENTATION ROADMAP (CODE CHANGES)

### Phase 1: Critical Code Quality (Weeks 1-2)
- [ ] Implement httpOnly cookie authentication
- [ ] Add Zod validation for all forms and API calls
- [ ] Set up error boundaries
- [ ] Configure CSP in Vite
- [ ] Add environment variable validation

### Phase 2: Testing Infrastructure (Weeks 3-4)
- [ ] Set up testing framework with @testing-library/react
- [ ] Configure MSW for API mocking
- [ ] Write unit tests for critical components (>70% coverage)
- [ ] Add accessibility tests

### Phase 3: Performance & Observability (Weeks 5-6)
- [ ] Implement code splitting
- [ ] Optimize bundle size
- [ ] Integrate Sentry error tracking
- [ ] Add Web Vitals monitoring
- [ ] Implement client-side logging

### Phase 4: Enhancement (Weeks 7-8)
- [ ] WCAG 2.1 AA compliance audit
- [ ] Prepare i18n infrastructure
- [ ] Optimize Redux state management
- [ ] Add comprehensive E2E tests

## 9. SUCCESS METRICS

**Pre-Production:**
- Test Coverage: ~5%
- TypeScript Strict: Partial
- Bundle Size: Unknown
- Error Tracking: None

**Post-Production:**
- Test Coverage: >70%
- TypeScript Strict: Full
- Bundle Size: <500KB
- Error Tracking: Sentry integrated
- Accessibility: WCAG 2.1 AA

## 10. CLOUD PROVIDER COMPARISON (CODE INTEGRATION)

### Alibaba Cloud (Primary)
- ARMS SDK integration for frontend monitoring
- OSS SDK for file uploads

### AWS Alternative
- CloudWatch RUM SDK
- S3 SDK for uploads

### GCP Alternative
- Cloud Monitoring SDK
- GCS SDK for uploads

---

**Note:** All infrastructure, deployment, Kubernetes, and operations concerns are documented in `PRODUCTION_READINESS_ALIBABA_INFRA.md`
```

---

## 2. PRODUCTION_READINESS_BACKEND_SERVICE.md

### Purpose
Code-level production readiness assessment for Node.js/TypeScript API service.

### Outline

```
# Production Readiness Assessment: Backend Service (Application Code)

## Executive Summary
- Application Overview
- Tech Stack (Node.js 20, TypeScript 5.3, Express, MongoDB)
- Code Quality Score: X/100
- Key Code-Level Gaps

## Code Quality Metrics
| Category | Score | Focus Area |
|----------|-------|------------|
| Type Safety | X/100 | TypeScript strict, Zod validation |
| Testing | X/100 | Unit, integration, E2E tests |
| Error Handling | X/100 | Standardized error hierarchy |
| Architecture | X/100 | DDD, clean architecture |
| Security (Code) | X/100 | Input validation, auth logic |
| Dependency Management | X/100 | Dependency injection, modularity |
| Logging | X/100 | Structured logging |

## 1. ARCHITECTURE & CODE ORGANIZATION

### 1.1 Domain-Driven Design (DDD)
- Current: Partial DDD implementation
- Required: Full domain model separation
- Structure:
  ```
  src/
  ├── domain/          # Domain models, entities, value objects
  ├── application/     # Use cases, application services
  ├── infrastructure/  # External services, repositories
  └── interfaces/      # Controllers, routes, DTOs
  ```

### 1.2 Multi-Tenant Architecture Review
- Current: Tenant isolation via middleware
- Analysis: Verify proper isolation
- Code Review:
  ```typescript
  // Tenant middleware validation
  // Database query isolation
  ```

### 1.3 Repository Pattern
- Current: Basic repository implementation
- Required: Complete repository abstraction
- Implementation:
  ```typescript
  // IHotelRepository interface
  // MongoHotelRepository implementation
  // Mock repository for testing
  ```

### 1.4 Dependency Injection
- Current: Manual dependency management
- Required: InversifyJS container
- Setup:
  ```typescript
  // Container configuration
  // Service registration
  // Constructor injection
  ```

## 2. TYPE SAFETY & VALIDATION

### 2.1 TypeScript Configuration
- Current: TypeScript 5.3
- Required: Strict mode enabled
- Configuration:
  ```json
  {
    "compilerOptions": {
      "strict": true,
      "strictNullChecks": true,
      "noUncheckedIndexedAccess": true,
      "exactOptionalPropertyTypes": true
    }
  }
  ```

### 2.2 Input Validation with Zod
- Current: Mixed validation approaches
- Required: Zod schemas for all inputs
- Examples:
  ```typescript
  // Request validation schemas
  // DTO validation
  // Environment variable validation
  ```

### 2.3 Branded Types
- Required: Type-safe IDs and domain values
- Implementation:
  ```typescript
  // Branded type definitions
  // Type guards
  // Usage examples
  ```

### 2.4 API Response Types
- Current: Inconsistent response typing
- Required: Standardized response wrapper
- Implementation:
  ```typescript
  // ApiResponse<T> type
  // Success/error response helpers
  ```

## 3. ERROR HANDLING

### 3.1 Custom Error Hierarchy
- Current: Basic error classes
- Required: Comprehensive error hierarchy
- Implementation:
  ```typescript
  // AppError base class
  // ValidationError
  // NotFoundError
  // UnauthorizedError
  // ExternalAPIError
  // DatabaseError
  ```

### 3.2 Global Error Handler
- Current: Basic Express error middleware
- Required: Comprehensive error handling
- Implementation:
  ```typescript
  // Error handler middleware
  // Error response formatting
  // Error logging integration
  ```

### 3.3 Error Recovery & Retry Logic
- Required: Automatic retry for transient failures
- Implementation:
  ```typescript
  // Retry wrapper with exponential backoff
  // Circuit breaker pattern
  ```

### 3.4 Correlation IDs
- Required: Request tracing across services
- Implementation:
  ```typescript
  // Correlation ID middleware
  // Logger integration
  // Response header inclusion
  ```

## 4. VENDOR INTEGRATION ARCHITECTURE

### 4.1 Adapter Pattern Review
- Current: Dingus, Hotetec, Roibos adapters
- Analysis: Ensure consistent interface
- Code Review:
  ```typescript
  // IHotelVendor interface
  // Adapter implementations
  // Error normalization
  ```

### 4.2 External API Error Handling
- Current: Basic error handling
- Required: Comprehensive fallback strategies
- Implementation:
  ```typescript
  // ExternalAPIError class
  // Retry logic per vendor
  // Circuit breaker integration
  ```

### 4.3 Response Normalization
- Current: Per-vendor response mapping
- Required: Consistent domain model mapping
- Code Review:
  ```typescript
  // Vendor response to domain model mapping
  // Validation of external data
  ```

### 4.4 Rate Limiting (Application Level)
- Required: Per-vendor rate limiting
- Implementation:
  ```typescript
  // Rate limiter utility
  // Vendor-specific limits
  ```

## 5. TESTING STRATEGY

### 5.1 Unit Testing
- Current Coverage: ~10%
- Required Coverage: >70%
- Focus Areas:
  - Domain model business logic
  - Validators
  - Utilities
  - Service layer
- Examples:
  ```typescript
  // Domain model tests
  // Service layer tests with mocks
  ```

### 5.2 Integration Testing
- Current: Minimal integration tests
- Required: Comprehensive API tests
- Setup:
  ```typescript
  // Supertest setup
  // Test database configuration
  // API endpoint tests
  ```

### 5.3 External Service Mocking
- Required: Mock all external vendors
- Tools:
  - nock for HTTP mocking
  - In-memory repositories for database
- Examples:
  ```typescript
  // Vendor API mocks
  // Repository mocks
  ```

### 5.4 E2E Testing
- Required: Critical user flows
- Focus:
  - Search → Book → Payment flow
  - Multi-tenant isolation
- Implementation:
  ```typescript
  // E2E test setup
  // Test scenarios
  ```

## 6. SECURITY (APPLICATION LEVEL)

### 6.1 Authentication & Authorization
- Current: JWT-based authentication
- Required: Enhanced security
- Improvements:
  ```typescript
  // JWT signing with RS256
  // Token rotation
  // Role-based access control middleware
  ```

### 6.2 Input Sanitization
- Required: SQL injection, NoSQL injection prevention
- Implementation:
  ```typescript
  // MongoDB query sanitization
  // Input sanitization utilities
  ```

### 6.3 Secrets Management (Code Level)
- Current: Environment variables
- Required: Validated configuration
- Implementation:
  ```typescript
  // Config validation with Zod
  // Type-safe configuration
  ```

### 6.4 Rate Limiting (Application Logic)
- Required: Express rate limiter with Redis
- Implementation:
  ```typescript
  // Rate limiter middleware
  // Redis connection
  // Per-route limits
  ```

### 6.5 CORS Configuration
- Current: Basic CORS
- Required: Strict CORS policy
- Configuration:
  ```typescript
  // CORS middleware with whitelist
  // Credential handling
  ```

## 7. LOGGING & OBSERVABILITY (APPLICATION SIDE)

### 7.1 Structured Logging
- Current: Basic console.log
- Required: Winston with JSON format
- Implementation:
  ```typescript
  // Logger configuration
  // Log levels
  // Context injection
  // Correlation ID integration
  ```

### 7.2 Request/Response Logging
- Required: HTTP request logging middleware
- Implementation:
  ```typescript
  // Morgan + Winston integration
  // Request sanitization (no passwords)
  ```

### 7.3 Performance Logging
- Required: Execution time tracking
- Implementation:
  ```typescript
  // Performance middleware
  // Slow query logging
  ```

### 7.4 Application Metrics (Code Integration)
- Required: Custom metrics for business logic
- Examples:
  ```typescript
  // Booking success/failure metrics
  // Vendor response time tracking
  ```

## 8. CONFIGURATION MANAGEMENT

### 8.1 Environment-Based Configuration
- Current: Hardcoded values in some places
- Required: Centralized configuration
- Implementation:
  ```typescript
  // Config service
  // Environment-specific configs
  // Zod validation
  ```

### 8.2 Feature Flags
- Required: Runtime feature toggling
- Implementation:
  ```typescript
  // Feature flag service
  // Usage in controllers
  ```

## 9. DATABASE (APPLICATION CODE)

### 9.1 Mongoose Schema Best Practices
- Current: Basic schemas
- Required: Comprehensive validation
- Improvements:
  ```typescript
  // Schema with validation
  // Indexes for performance
  // Timestamps
  // Soft deletes
  ```

### 9.2 Query Optimization
- Required: Efficient queries with proper indexes
- Code Review:
  ```typescript
  // Analyze slow queries
  // Add indexes
  // Use aggregation pipeline
  ```

### 9.3 Connection Pool Management
- Required: Optimal connection settings
- Configuration:
  ```typescript
  // Mongoose connection options
  // Pool size configuration
  ```

## 10. PAYMENT INTEGRATION (TropiPay)

### 10.1 TropiPay Service Review
- Current: Basic implementation
- Required: Production-ready error handling
- Code Review:
  ```typescript
  // Error handling
  // Webhook validation
  // Idempotency
  ```

### 10.2 Webhook Security
- Required: Signature verification
- Implementation:
  ```typescript
  // Webhook signature validation
  // Replay attack prevention
  ```

### 10.3 Payment State Machine
- Required: Clear payment status transitions
- Implementation:
  ```typescript
  // Payment status enum
  // State transition validation
  ```

## 11. IMPLEMENTATION ROADMAP (CODE CHANGES)

### Phase 1: Foundation (Weeks 1-2)
- [ ] Enable TypeScript strict mode
- [ ] Implement comprehensive Zod validation
- [ ] Set up InversifyJS dependency injection
- [ ] Create standardized error hierarchy
- [ ] Add correlation ID middleware

### Phase 2: Testing (Weeks 3-4)
- [ ] Set up Jest testing infrastructure
- [ ] Write unit tests for domain models (>70% coverage)
- [ ] Write integration tests for API endpoints
- [ ] Set up external service mocking (nock)
- [ ] Add E2E tests for critical flows

### Phase 3: Security & Observability (Weeks 5-6)
- [ ] Implement Winston structured logging
- [ ] Add request/response logging middleware
- [ ] Enhance JWT authentication (RS256, rotation)
- [ ] Add rate limiting with Redis
- [ ] Implement input sanitization

### Phase 4: Architecture & Performance (Weeks 7-8)
- [ ] Complete repository pattern implementation
- [ ] Add circuit breaker for external APIs
- [ ] Optimize database queries and indexes
- [ ] Implement feature flags
- [ ] Add performance tracking

## 12. SUCCESS METRICS

**Pre-Production:**
- Test Coverage: ~10%
- TypeScript Strict: Partial
- Error Handling: Basic
- Logging: console.log
- Dependency Injection: Manual

**Post-Production:**
- Test Coverage: >70%
- TypeScript Strict: Full
- Error Handling: Comprehensive hierarchy
- Logging: Winston structured JSON
- Dependency Injection: InversifyJS

## 13. CLOUD PROVIDER SDK COMPARISON (CODE INTEGRATION)

### Alibaba Cloud (Primary)
- @alicloud/pop-core for API access
- ali-oss for object storage
- Connection to ApsaraDB for MongoDB

### AWS Alternative
- aws-sdk for services
- S3 client
- DocumentDB connection

### GCP Alternative
- @google-cloud/* packages
- GCS client
- MongoDB Atlas connection

---

**Note:** All infrastructure, database provisioning, Kubernetes, and operations concerns are documented in `PRODUCTION_READINESS_ALIBABA_INFRA.md`
```

---

## 3. PRODUCTION_READINESS_BACKOFFICE_APP.md

### Purpose
Code-level production readiness for admin dashboard with enhanced security focus.

### Outline

```
# Production Readiness Assessment: Backoffice App (Application Code)

## Executive Summary
- Application Overview
- Tech Stack (React 18.2, TypeScript 5.3, Vite 5.0, Tailwind + Radix UI)
- Code Quality Score: X/100
- Admin Security Requirements (Stricter than Customer Apps)

## Code Quality Metrics
| Category | Score | Focus Area |
|----------|-------|------------|
| Type Safety | X/100 | TypeScript strict, API types |
| Testing | X/100 | Component tests, admin flow tests |
| Security (Code) | X/100 | MFA, RBAC, audit logging |
| Error Handling | X/100 | Error boundaries, admin alerts |
| Accessibility | X/100 | Admin panel accessibility |
| Maintainability | X/100 | Component architecture |

## 1. ADMIN AUTHENTICATION & SECURITY

### 1.1 Multi-Factor Authentication (MFA)
- Current: Not implemented
- Required: MANDATORY for all admin users
- Implementation:
  ```typescript
  // speakeasy integration
  // QR code generation
  // MFA verification component
  // Backup codes
  ```

### 1.2 Enhanced Token Security
- Current: localStorage (XSS vulnerable)
- Required: httpOnly cookies + MFA
- Implementation:
  ```typescript
  // Cookie-based auth for admin
  // Secure, HttpOnly, SameSite=Strict
  ```

### 1.3 Session Management
- Required: Auto-timeout for admin sessions
- Configuration:
  - Idle timeout: 15 minutes
  - Absolute timeout: 8 hours
  - Re-authentication for sensitive operations
- Implementation:
  ```typescript
  // Session timeout hook
  // Activity tracker
  // Re-authentication modal
  ```

### 1.4 IP Allowlisting (Application Side)
- Required: Display allowed IP status
- Implementation:
  ```typescript
  // IP detection component
  // Access denied UI
  ```

## 2. ROLE-BASED ACCESS CONTROL (RBAC)

### 2.1 Fine-Grained Permissions System
- Current: Basic role enum (ADMIN, SALES_AGENT, etc.)
- Required: Permission-based access control
- Implementation:
  ```typescript
  // Permission enum
  // Role-to-permission mapping
  // usePermission hook
  // ProtectedAction component
  // ProtectedRoute component
  ```

### 2.2 Permission Checking
- Required: UI-level and API-level permission checks
- Examples:
  ```typescript
  // Permission hook usage
  // Conditional rendering based on permissions
  // API call authorization
  ```

### 2.3 Role Management UI
- Required: Admin interface for role assignment
- Components:
  - Role list
  - Permission matrix
  - User role assignment
- Implementation:
  ```typescript
  // RoleManagement component
  // PermissionMatrix component
  ```

## 3. AUDIT LOGGING (APPLICATION SIDE)

### 3.1 Comprehensive Action Logging
- Current: No audit logging
- Required: Log ALL admin actions
- Logged Information:
  - User ID, email
  - Action type (CREATE, UPDATE, DELETE, etc.)
  - Resource type and ID
  - Changes (before/after)
  - IP address
  - User agent
  - Timestamp
  - Success/failure
- Implementation:
  ```typescript
  // AuditLogEntry interface
  // logAdminAction utility
  // Integration with API calls
  ```

### 3.2 Audit Log UI
- Required: Searchable audit log viewer
- Features:
  - Filter by user, action, resource, date range
  - Export to CSV
  - Drill-down to view details
- Implementation:
  ```typescript
  // AuditLog component
  // Filter controls
  // Export functionality
  ```

### 3.3 Critical Action Confirmation
- Required: Two-step confirmation for destructive actions
- Examples:
  - Delete hotel
  - Cancel booking
  - Refund payment
  - Delete user
- Implementation:
  ```typescript
  // ConfirmationModal component
  // Re-authentication for critical actions
  ```

## 4. TYPE SAFETY & VALIDATION

### 4.1 TypeScript Strict Mode
- Current: TypeScript 5.3
- Required: Full strict mode
- Configuration:
  ```json
  {
    "compilerOptions": {
      "strict": true,
      "strictNullChecks": true,
      "noUncheckedIndexedAccess": true
    }
  }
  ```

### 4.2 API Type Generation
- Required: Type-safe admin API client
- Implementation:
  ```typescript
  // Admin API types from OpenAPI
  // Type-safe request/response handling
  ```

### 4.3 Form Validation
- Current: Basic validation
- Required: Zod schema validation for all admin forms
- Examples:
  ```typescript
  // Hotel creation/update schemas
  // User management schemas
  // Booking modification schemas
  ```

## 5. COMPONENT ARCHITECTURE & TESTING

### 5.1 Admin Component Organization
- Required: Clear separation of concerns
- Structure:
  ```
  src/
  ├── features/
  │   ├── hotels/
  │   ├── bookings/
  │   ├── users/
  │   └── analytics/
  ├── components/
  │   ├── admin/        # Admin-specific components
  │   ├── common/       # Shared components
  │   └── forms/        # Form components
  └── layouts/
      └── AdminLayout/
  ```

### 5.2 Testing Strategy
- Current Coverage: <5%
- Required Coverage: >60% (lower than customer app, higher security focus)
- Focus Areas:
  - RBAC permission checks
  - Audit logging integration
  - MFA flow
  - Critical admin operations
- Examples:
  ```typescript
  // Permission-based rendering tests
  // MFA flow tests
  // Audit logging tests
  ```

### 5.3 Error Boundaries
- Required: Admin-friendly error handling
- Implementation:
  ```typescript
  // AdminErrorBoundary component
  // Error reporting to admin monitoring
  ```

## 6. SECURITY (APPLICATION LEVEL)

### 6.1 Input Sanitization
- Required: Prevent XSS in admin panel
- Implementation:
  ```typescript
  // DOMPurify integration
  // Sanitize user-generated content
  ```

### 6.2 Content Security Policy (Build Config)
- Required: Strict CSP for admin app
- Vite Configuration:
  ```typescript
  // vite.config.ts CSP headers
  // Nonce-based script execution
  ```

### 6.3 Sensitive Data Masking
- Required: Mask PII in admin UI
- Examples:
  - Mask credit card numbers
  - Mask email addresses (partial)
  - Mask phone numbers
- Implementation:
  ```typescript
  // Data masking utilities
  // Conditional reveal components
  ```

## 7. ADMIN-SPECIFIC UI/UX

### 7.1 Data Tables & Filters
- Required: Efficient admin data management
- Features:
  - Server-side pagination
  - Advanced filtering
  - Sorting
  - Bulk actions
  - Export functionality
- Implementation:
  ```typescript
  // DataTable component with server-side features
  // Filter builder
  ```

### 7.2 Admin Dashboard
- Required: Key metrics and alerts
- Widgets:
  - Recent bookings
  - Failed payments
  - User activity
  - System alerts
- Implementation:
  ```typescript
  // Dashboard component
  // Widget system
  ```

### 7.3 Accessibility for Admin Tools
- Required: WCAG 2.1 AA compliance
- Focus:
  - Keyboard navigation for data tables
  - Screen reader support
  - High contrast mode

## 8. PERFORMANCE (CODE LEVEL)

### 8.1 Code Splitting
- Required: Route-based and component-based splitting
- Implementation:
  ```typescript
  // React.lazy for admin routes
  // Suspense boundaries
  ```

### 8.2 Bundle Optimization
- Target: <600KB (admin app can be slightly larger)
- Optimizations:
  - Remove unused dependencies
  - Tree-shaking
  - Bundle analysis

### 8.3 Data Caching
- Required: RTK Query caching for admin data
- Configuration:
  ```typescript
  // Cache tag system
  // Invalidation on mutations
  ```

## 9. LOGGING & ERROR REPORTING (APPLICATION SIDE)

### 9.1 Client-Side Logging
- Required: Structured logging for admin actions
- Implementation:
  ```typescript
  // Logger utility
  // Admin action logging
  ```

### 9.2 Error Tracking
- Required: Sentry or equivalent for admin app
- Configuration:
  ```typescript
  // Sentry.init() with admin context
  // User identification
  ```

## 10. IMPLEMENTATION ROADMAP (CODE CHANGES)

### Phase 1: Critical Security (Weeks 1-2)
- [ ] Implement MFA for all admin users
- [ ] Migrate to httpOnly cookies
- [ ] Implement session timeout (15 minutes idle)
- [ ] Add environment variable validation with Zod
- [ ] Configure CSP in Vite

### Phase 2: RBAC & Audit Logging (Weeks 3-4)
- [ ] Implement fine-grained permission system
- [ ] Create RBAC components (ProtectedAction, ProtectedRoute)
- [ ] Implement comprehensive audit logging
- [ ] Build audit log viewer UI
- [ ] Add critical action confirmation modals

### Phase 3: Testing & Error Handling (Weeks 5-6)
- [ ] Set up testing framework
- [ ] Write tests for RBAC (>60% coverage)
- [ ] Test MFA flow
- [ ] Add error boundaries
- [ ] Integrate Sentry error tracking

### Phase 4: Enhancement (Weeks 7-8)
- [ ] Build admin dashboard with key metrics
- [ ] Implement advanced data tables
- [ ] Add data export functionality
- [ ] WCAG 2.1 AA compliance
- [ ] Code splitting and bundle optimization

## 11. SUCCESS METRICS

**Pre-Production:**
- MFA: Not implemented
- RBAC: Basic role enum
- Audit Logging: None
- Test Coverage: <5%
- Session Management: Basic JWT

**Post-Production:**
- MFA: 100% of admin users
- RBAC: Fine-grained permissions
- Audit Logging: All actions logged
- Test Coverage: >60%
- Session Management: httpOnly cookies, 15-min timeout, re-auth for sensitive ops

## 12. CLOUD PROVIDER SDK COMPARISON (CODE INTEGRATION)

### Alibaba Cloud (Primary)
- ARMS SDK for admin app monitoring
- SLS SDK for audit log shipping

### AWS Alternative
- CloudWatch RUM for monitoring
- CloudWatch Logs SDK

### GCP Alternative
- Cloud Monitoring SDK
- Cloud Logging SDK

---

**Note:** All infrastructure, IP allowlisting (Network Policies), Kubernetes, and operations concerns are documented in `PRODUCTION_READINESS_ALIBABA_INFRA.md`
```

---

## 4. PRODUCTION_READINESS_ALIBABA_INFRA.md

### Purpose
Complete infrastructure, Kubernetes, and operations production readiness assessment.

### Outline

```
# Production Readiness Assessment: Alibaba Cloud Infrastructure

## Executive Summary
- Infrastructure Overview
- Current State: Development, Not Production Ready
- Infrastructure Score: X/100
- Critical Security Vulnerability: Hardcoded credentials

## Infrastructure Quality Metrics
| Category | Score | Focus Area |
|----------|-------|------------|
| Security | X/100 | Credentials, secrets, access control |
| Reliability | X/100 | Multi-AZ, HA, disaster recovery |
| Availability | X/100 | Load balancing, autoscaling |
| Observability | X/100 | Monitoring, logging, tracing |
| Automation | X/100 | IaC, GitOps, CI/CD |
| Cost Optimization | X/100 | Resource efficiency |

## 1. CRITICAL SECURITY ISSUES

### 1.1 🚨 Hardcoded Credentials in Git
- **CRITICAL VULNERABILITY**
- Location: `terraform.tfvars`
- Issue:
  ```terraform
  access_key = "LTAI5tDAQrJSgkpERpE1zf31"
  secret_key = "vrrAM0uXkLpnUMlsFLrE5TPkoocw2I"
  ```
- Impact: Full access to Alibaba Cloud account
- **IMMEDIATE ACTIONS REQUIRED:**
  1. Revoke compromised credentials in Alibaba Cloud console
  2. Remove from Git history with BFG Repo Cleaner
  3. Add terraform.tfvars to .gitignore
  4. Never commit terraform.tfvars again
  5. Use environment variables or Terraform Cloud
  6. Rotate all secrets

### 1.2 Secret Management Migration
- Current: Environment variables
- Required: Alibaba Cloud Secret Manager
- Implementation:
  ```terraform
  # Terraform for Secret Manager
  # Application integration
  # Secret rotation
  ```

### 1.3 Network Security
- Required: Private VPC, security groups
- Implementation:
  ```terraform
  # VPC with private subnets
  # Security group rules
  # Network ACLs
  ```

## 2. INFRASTRUCTURE AS CODE (TERRAFORM)

### 2.1 Current Terraform State
- File: `main.tf`
- Resources: VPC, ACK cluster, basic networking
- Gaps: No backend state, no modules, no multi-AZ

### 2.2 Terraform Best Practices
- Required:
  - Remote state in OSS with locking
  - Modular structure
  - Variable validation
  - Output definitions
  - Terraform workspaces (dev/staging/prod)
- Implementation:
  ```terraform
  # Backend configuration
  # Module structure
  # Variable validation
  ```

### 2.3 Multi-AZ Architecture
- Current: Single AZ
- Required: Multi-AZ for high availability
- Resources:
  - VPC across 3 AZs
  - ACK node groups in multiple AZs
  - ApsaraDB with multi-AZ replication
- Implementation:
  ```terraform
  # Multi-AZ VPC
  # ACK cluster with multi-AZ node pools
  # Database with multi-AZ standby
  ```

## 3. ALIBABA CONTAINER SERVICE FOR KUBERNETES (ACK)

### 3.1 ACK Cluster Configuration
- Current: Basic cluster
- Required: Production-grade configuration
- Settings:
  - Managed cluster (not dedicated)
  - Kubernetes version: 1.28+
  - Node pool autoscaling
  - RBAC enabled
  - Pod security policies
- Terraform:
  ```terraform
  # ACK cluster resource
  # Node pool configuration
  # Autoscaling settings
  ```

### 3.2 Node Pool Strategy
- Required: Multiple node pools
- Pools:
  - General workload pool (2-10 nodes)
  - Database workload pool (2-5 nodes)
  - System pool (2 nodes)
- Configuration:
  ```terraform
  # Node pool definitions
  # Instance types
  # Autoscaling ranges
  ```

## 4. SSL/TLS CERTIFICATE AUTOMATION

### 4.1 cert-manager Installation
- Purpose: Automated SSL certificate management
- Provider: Let's Encrypt (ACME)
- Installation:
  ```terraform
  # Helm release for cert-manager
  # CRD installation
  ```

### 4.2 ClusterIssuer Configuration
- Required: Production Let's Encrypt issuer
- Challenge Types:
  - HTTP-01 for public endpoints
  - DNS-01 for wildcard certificates
- Implementation:
  ```terraform
  # ClusterIssuer resource
  # Alibaba DNS integration
  ```

### 4.3 Certificate Resources
- Required: Per-application certificates
- Examples:
  ```yaml
  # agency-app certificate
  # backend-service certificate
  # backoffice-app certificate
  ```

### 4.4 Wildcard Certificate
- Optional: *.lukzen-op.com
- Configuration:
  ```yaml
  # Wildcard certificate with DNS-01
  ```

## 5. DNS AUTOMATION

### 5.1 external-dns Installation
- Purpose: Automated DNS record management
- Provider: Alibaba Cloud DNS
- Installation:
  ```terraform
  # Helm release for external-dns
  # RBAC configuration
  ```

### 5.2 external-dns Configuration
- Settings:
  - Domain filter: lukzen-op.com
  - Policy: sync (create and delete records)
  - TXT registry for ownership
- Implementation:
  ```terraform
  # Helm values
  # DNS provider credentials
  ```

### 5.3 Service Annotations
- Required: Annotate services for DNS automation
- Examples:
  ```yaml
  # Agency app ingress with external-dns annotation
  # Backend service ingress
  # Backoffice app ingress
  ```

## 6. APPLICATION DEPLOYMENTS

### 6.1 Agency App Deployment
- Kubernetes Resources:
  - Deployment (2-8 replicas)
  - Service (ClusterIP)
  - Ingress (with cert-manager + external-dns annotations)
  - HorizontalPodAutoscaler
  - ConfigMap for environment variables
  - Secret for sensitive data
- Implementation:
  ```yaml
  # Complete K8s manifests
  # HPA configuration
  # Ingress with TLS
  ```

### 6.2 Backend Service Deployment
- Kubernetes Resources:
  - Deployment (3-15 replicas)
  - Service (ClusterIP)
  - Ingress
  - HorizontalPodAutoscaler
  - ConfigMap
  - Secret
  - PodDisruptionBudget
- Health Checks:
  ```yaml
  livenessProbe:
    httpGet:
      path: /health/live
      port: 3000
    initialDelaySeconds: 30
    periodSeconds: 10
  readinessProbe:
    httpGet:
      path: /health/ready
      port: 3000
    initialDelaySeconds: 10
    periodSeconds: 5
  ```
- Implementation:
  ```yaml
  # Complete K8s manifests
  # HPA with CPU and memory targets
  ```

### 6.3 Backoffice App Deployment
- Kubernetes Resources:
  - Deployment (2-5 replicas)
  - Service (ClusterIP)
  - Ingress
  - HorizontalPodAutoscaler
  - NetworkPolicy (IP allowlisting)
  - ConfigMap
  - Secret
- Network Policy:
  ```yaml
  # IP allowlisting for admin access
  # VPN-only access
  ```
- Implementation:
  ```yaml
  # Complete K8s manifests
  # Restricted network policy
  ```

### 6.4 Ingress Controller
- Required: NGINX Ingress Controller
- Installation:
  ```terraform
  # Helm release for ingress-nginx
  # LoadBalancer configuration
  ```
- Configuration:
  - SSL passthrough
  - Rate limiting
  - Client IP preservation

## 7. MANAGED DATABASES

### 7.1 ApsaraDB for MongoDB
- Current: Self-hosted MongoDB in K8s (not recommended)
- Required: Managed ApsaraDB for MongoDB
- Configuration:
  - Multi-AZ deployment
  - Automatic backups (daily)
  - Point-in-time recovery
  - Monitoring integration
- Terraform:
  ```terraform
  # ApsaraDB for MongoDB instance
  # Replica set configuration
  # Backup policy
  # Network access control
  ```
- Cost: ~$150-400/month

### 7.2 ApsaraDB for Redis
- Required: Managed Redis for caching and sessions
- Configuration:
  - Multi-AZ with auto-failover
  - Persistence enabled
  - Eviction policy: allkeys-lru
- Terraform:
  ```terraform
  # ApsaraDB for Redis instance
  # HA configuration
  ```
- Cost: ~$50-150/month

### 7.3 Database Connection from K8s
- Required: Secure connectivity
- Implementation:
  - Private VPC endpoint
  - Kubernetes secrets for credentials
  - Connection pooling in application
- Examples:
  ```yaml
  # Secret for database credentials
  # ConfigMap for connection strings
  ```

## 8. MONITORING & OBSERVABILITY

### 8.1 Alibaba Cloud ARMS
- Purpose: Application Real-Time Monitoring Service
- Features:
  - Application performance monitoring (APM)
  - Distributed tracing
  - Custom metrics
  - Alerting
- Setup:
  ```terraform
  # ARMS application monitoring
  # Agent configuration
  ```
- Integration:
  - Backend service instrumentation
  - Frontend RUM for React apps
- Cost: ~$80-200/month

### 8.2 Prometheus + Grafana
- Purpose: Kubernetes cluster and infrastructure monitoring
- Installation:
  ```terraform
  # kube-prometheus-stack Helm release
  # Grafana dashboards
  # Prometheus rules
  ```
- Dashboards:
  - Cluster overview
  - Node metrics
  - Pod metrics
  - Application-specific dashboards
- Cost: Infrastructure only (runs in K8s)

### 8.3 CloudMonitor Integration
- Purpose: Alibaba Cloud resource monitoring
- Monitored Resources:
  - ACK cluster
  - EIP bandwidth
  - SLB
  - ApsaraDB instances
- Terraform:
  ```terraform
  # CloudMonitor alert rules
  ```

### 8.4 Custom Metrics & Dashboards
- Required: Business metrics
- Examples:
  - Booking success rate
  - Payment success rate
  - Vendor API latency
  - Search query performance
- Implementation:
  ```yaml
  # ServiceMonitor resources
  # Grafana dashboard JSON
  ```

## 9. LOGGING

### 9.1 Alibaba Simple Log Service (SLS)
- Purpose: Centralized logging for all applications
- Log Sources:
  - Application logs (agency-app, backend-service, backoffice-app)
  - Kubernetes audit logs
  - Ingress access logs
  - Database slow query logs
- Setup:
  ```terraform
  # SLS project and logstore
  # Logtail configuration
  ```
- Cost: ~$60-150/month

### 9.2 Fluentd/Fluent Bit Deployment
- Purpose: Log collection from Kubernetes
- Installation:
  ```terraform
  # Fluent Bit DaemonSet
  # Output to SLS
  ```
- Configuration:
  - Pod logs
  - Container logs
  - System logs

### 9.3 Audit Logging (Admin Actions)
- Purpose: Track all admin actions from backoffice-app
- Implementation:
  - SLS logstore for audit logs
  - Retention: 90 days minimum
  - Searchable with SQL
- Setup:
  ```terraform
  # Audit log logstore
  # Index configuration
  ```

### 9.4 Log Retention & Analysis
- Retention Policies:
  - Application logs: 30 days
  - Audit logs: 90 days
  - Access logs: 7 days
- Analysis:
  - SLS SQL queries
  - Dashboards for log patterns
  - Alerts for errors

## 10. BACKUP & DISASTER RECOVERY

### 10.1 Velero Installation
- Purpose: Kubernetes backup and disaster recovery
- Installation:
  ```terraform
  # Helm release for Velero
  # OSS bucket for backups
  ```
- Backup Scope:
  - All Kubernetes resources
  - Persistent volumes
  - Namespaces

### 10.2 Backup Schedule
- Required: Automated backups
- Schedules:
  - Daily backups (retain 7 days)
  - Weekly backups (retain 4 weeks)
  - Monthly backups (retain 12 months)
- Configuration:
  ```yaml
  # Velero schedule resources
  ```

### 10.3 Database Backups
- ApsaraDB Automated Backups:
  - Daily full backups
  - Continuous backup (point-in-time recovery)
  - Retention: 7 days
- Manual Backups:
  - Before major changes
  - Before migrations

### 10.4 Disaster Recovery Plan
- RPO (Recovery Point Objective): 1 hour
- RTO (Recovery Time Objective): 4 hours
- DR Procedures:
  1. Restore from Velero backup
  2. Restore database from ApsaraDB backup
  3. Verify application functionality
  4. Update DNS if necessary
- Documentation:
  ```markdown
  # DR runbook
  ```

## 11. AUTOSCALING

### 11.1 Horizontal Pod Autoscaler (HPA)
- Applications:
  - Agency App: 2-8 replicas (CPU: 70%, Memory: 80%)
  - Backend Service: 3-15 replicas (CPU: 60%, Memory: 75%)
  - Backoffice App: 2-5 replicas (CPU: 70%)
- Configuration:
  ```yaml
  # HPA resources per application
  ```

### 11.2 Vertical Pod Autoscaler (VPA)
- Optional: Automatic resource request adjustment
- Installation:
  ```terraform
  # VPA Helm release
  ```
- Usage: Development and staging environments

### 11.3 Cluster Autoscaler
- Required: ACK node pool autoscaling
- Configuration:
  - Min nodes: 2-3 per pool
  - Max nodes: 10-15 per pool
  - Scale-down delay: 10 minutes
- Terraform:
  ```terraform
  # Autoscaling configuration in node pool
  ```

## 12. NETWORKING & LOAD BALANCING

### 12.1 Virtual Private Cloud (VPC)
- Required: Multi-AZ VPC
- Configuration:
  - CIDR: 10.0.0.0/16
  - Public subnets: 10.0.1.0/24, 10.0.2.0/24, 10.0.3.0/24
  - Private subnets: 10.0.11.0/24, 10.0.12.0/24, 10.0.13.0/24
- Terraform:
  ```terraform
  # VPC resource
  # Subnets across AZs
  # Route tables
  ```

### 12.2 Server Load Balancer (SLB)
- Purpose: Ingress traffic load balancing
- Configuration:
  - Type: Application Load Balancer (Layer 7)
  - Listener: HTTPS (443) with SSL offloading
  - Health checks
  - Sticky sessions
- Terraform:
  ```terraform
  # SLB instance
  # Listeners
  # Backend servers (K8s nodes)
  ```

### 12.3 Elastic IP (EIP)
- Required: Static IP for SLB
- Configuration:
  - Bandwidth: 100 Mbps (adjustable)
  - Associated with SLB
- Terraform:
  ```terraform
  # EIP resource
  # EIP association
  ```

### 12.4 Network Policies
- Required: Pod-to-pod communication control
- Policies:
  - Backoffice app: IP allowlisting (VPN only)
  - Backend service: Allow from agency-app and backoffice-app
  - Default deny all
- Implementation:
  ```yaml
  # NetworkPolicy resources
  ```

## 13. SECURITY HARDENING

### 13.1 RBAC (Kubernetes)
- Required: Least privilege access
- Roles:
  - Admin role (full access)
  - Developer role (namespace-scoped)
  - CI/CD role (deployment only)
- Implementation:
  ```yaml
  # ClusterRole and RoleBinding resources
  ```

### 13.2 Pod Security Standards
- Required: Enforce pod security
- Level: Restricted
- Enforcement:
  - No privileged containers
  - Read-only root filesystem
  - Drop all capabilities
  - Run as non-root
- Implementation:
  ```yaml
  # Pod Security Standards enforcement
  # SecurityContext in deployments
  ```

### 13.3 Image Scanning
- Required: Scan container images for vulnerabilities
- Tools:
  - Alibaba Container Registry security scan
  - Trivy in CI/CD pipeline
- Policy: No HIGH or CRITICAL vulnerabilities

### 13.4 Secret Encryption at Rest
- Required: Kubernetes secret encryption
- Configuration:
  - ACK secret encryption enabled
  - KMS integration
- Terraform:
  ```terraform
  # ACK cluster encryption config
  ```

## 14. CI/CD INTEGRATION

### 14.1 Container Registry
- Current: Alibaba Container Registry (ACR)
- Configuration:
  - Private registry
  - Image scanning enabled
  - Automated builds (optional)
- Terraform:
  ```terraform
  # ACR namespace
  # Repository creation
  ```

### 14.2 GitOps with ArgoCD (Recommended)
- Purpose: Declarative continuous delivery
- Installation:
  ```terraform
  # ArgoCD Helm release
  ```
- Workflow:
  - Git repository as source of truth
  - Automated sync to K8s cluster
  - Rollback capabilities

### 14.3 CI/CD Pipeline Structure
- Build:
  - Run tests
  - Build Docker image
  - Scan for vulnerabilities
  - Push to ACR
- Deploy:
  - Update K8s manifests
  - ArgoCD auto-sync OR manual kubectl apply
  - Health checks
  - Rollback on failure

## 15. COST OPTIMIZATION

### 15.1 Resource Right-Sizing
- Required: Optimize resource requests/limits
- Tools:
  - VPA recommendations
  - ARMS performance data
- Actions:
  - Review CPU/memory usage weekly
  - Adjust requests/limits based on data

### 15.2 Reserved Instances
- Recommendation: Purchase reserved instances for baseline capacity
- Savings: 30-50% for 1-year commitment

### 15.3 Spot Instances (Development/Staging)
- Use case: Non-production workloads
- Savings: Up to 90%
- Configuration:
  ```terraform
  # Spot instance node pool
  ```

### 15.4 Auto-Shutdown for Dev Environments
- Recommendation: Shut down dev/staging during off-hours
- Implementation:
  - CronJob to scale down deployments
  - Save ~50% on dev costs

## 16. COMPLIANCE & GOVERNANCE

### 16.1 Tagging Strategy
- Required: Consistent resource tagging
- Tags:
  - Environment (dev/staging/prod)
  - Application (agency-app, backend-service, etc.)
  - Owner
  - Cost center
- Terraform:
  ```terraform
  # Default tags in provider
  # Resource-specific tags
  ```

### 16.2 Access Control
- Required: IAM policies for least privilege
- Policies:
  - Developer access (read-only prod)
  - DevOps access (full access)
  - CI/CD service account
- Alibaba RAM:
  ```terraform
  # RAM users and policies
  ```

### 16.3 Audit Logging (Infrastructure)
- Required: Alibaba ActionTrail
- Logged Actions:
  - All API calls
  - Resource changes
  - Access attempts
- Retention: 90 days minimum
- Terraform:
  ```terraform
  # ActionTrail configuration
  ```

## 17. IMPLEMENTATION ROADMAP

### Phase 1: Critical Security (Weeks 1-2)
- [ ] 🚨 Revoke and remove hardcoded credentials immediately
- [ ] Set up Terraform remote state in OSS
- [ ] Migrate secrets to Secret Manager
- [ ] Create multi-AZ VPC
- [ ] Configure network security groups
- [ ] Enable ACK secret encryption

### Phase 2: SSL/DNS Automation (Weeks 3-4)
- [ ] Install cert-manager in ACK
- [ ] Configure Let's Encrypt ClusterIssuers
- [ ] Install external-dns
- [ ] Configure DNS provider integration
- [ ] Update ingress resources with annotations
- [ ] Test certificate issuance and renewal

### Phase 3: Managed Databases (Weeks 5-6)
- [ ] Provision ApsaraDB for MongoDB
- [ ] Configure multi-AZ replication
- [ ] Migrate data from self-hosted MongoDB
- [ ] Provision ApsaraDB for Redis
- [ ] Update application configurations
- [ ] Configure automated backups

### Phase 4: Application Deployments (Weeks 7-8)
- [ ] Create production K8s manifests for agency-app
- [ ] Create production K8s manifests for backend-service
- [ ] Create production K8s manifests for backoffice-app
- [ ] Configure HPA for all applications
- [ ] Set up Network Policies
- [ ] Configure Pod Security Standards

### Phase 5: Monitoring & Logging (Weeks 9-10)
- [ ] Set up ARMS application monitoring
- [ ] Install Prometheus + Grafana stack
- [ ] Configure SLS for centralized logging
- [ ] Deploy Fluent Bit for log collection
- [ ] Create monitoring dashboards
- [ ] Configure alerting rules

### Phase 6: Backup & DR (Weeks 11-12)
- [ ] Install Velero
- [ ] Configure backup schedules
- [ ] Test backup and restore procedures
- [ ] Document DR runbook
- [ ] Conduct DR drill

## 18. COST ANALYSIS

### Monthly Cost Breakdown (Production)

| Resource | Alibaba Cloud | AWS | GCP |
|----------|--------------|-----|-----|
| **ACK/EKS/GKE Cluster** | $120 | $150 | $75 |
| **Node Pools (avg)** | $350-800 | $400-900 | $320-750 |
| **ApsaraDB/DocumentDB/MongoDB** | $150-400 | $200-500 | $180-450 |
| **Redis** | $50-150 | $60-180 | $45-140 |
| **Load Balancer** | $30-60 | $35-70 | $25-55 |
| **Bandwidth/EIP** | $50-100 | $60-120 | $40-90 |
| **Monitoring (ARMS/CloudWatch)** | $80-200 | $100-250 | $60-180 |
| **Logging (SLS)** | $60-150 | $80-200 | $40-120 |
| **Backup (Velero + DB)** | $30-80 | $40-100 | $25-75 |
| **Certificates** | $0 (Let's Encrypt) | $0 | $0 |
| **DNS** | $5-10 | $5-10 | $5-10 |
| **Container Registry** | $20-50 | $25-60 | $15-40 |
| **Secrets Manager** | $10-20 | $15-30 | $10-20 |
| **TOTAL** | **$955-2,220/mo** | **$1,170-2,570/mo** | **$840-2,000/mo** |

**One-Time Implementation Cost:**
- Infrastructure setup: $25,000-35,000
- Migration effort: $15,000-20,000
- Security audit: $5,000-10,000
- **Total:** $45,000-65,000

## 19. CLOUD PROVIDER COMPARISON

### Why Stay with Alibaba Cloud
- ✅ Already invested in infrastructure
- ✅ Good presence in Asia-Pacific
- ✅ Competitive pricing
- ✅ Managed Kubernetes (ACK) is mature
- ✅ Team familiarity

### AWS Alternative
- ✅ Most mature cloud provider
- ✅ Widest service offering
- ✅ Best third-party integrations
- ❌ Higher cost (15-20% more expensive)
- ❌ Complexity can be overwhelming

### GCP Alternative
- ✅ Superior Kubernetes (GKE Autopilot)
- ✅ Best-in-class logging/monitoring
- ✅ Lowest cost for compute
- ✅ Excellent BigQuery for audit log analysis
- ❌ Smaller service catalog
- ❌ Less presence in Asia-Pacific

**Recommendation:** **Stay with Alibaba Cloud** for now. Reevaluate when expanding to Europe/Americas.

## 20. SUCCESS METRICS

### Pre-Production
- Hardcoded Credentials: ❌ In Git
- Multi-AZ: ❌ Single AZ
- Managed Databases: ❌ Self-hosted in K8s
- SSL Automation: ❌ Manual certificates
- DNS Automation: ❌ Manual DNS records
- Monitoring: ❌ None
- Logging: ❌ None
- Backup: ❌ No backup strategy
- Autoscaling: ❌ No HPA

### Post-Production
- Hardcoded Credentials: ✅ Revoked, using Secret Manager
- Multi-AZ: ✅ 3 AZs with HA
- Managed Databases: ✅ ApsaraDB for MongoDB + Redis
- SSL Automation: ✅ cert-manager with Let's Encrypt
- DNS Automation: ✅ external-dns
- Monitoring: ✅ ARMS + Prometheus + Grafana
- Logging: ✅ SLS with 30-day retention
- Backup: ✅ Velero daily backups + DB snapshots
- Autoscaling: ✅ HPA for all apps

---

**Note:** All application code quality improvements are documented in:
- `CODE_QUALITY_ASSESSMENT.md` (cross-cutting patterns)
- `PRODUCTION_READINESS_AGENCY_APP.md` (agency-app code)
- `PRODUCTION_READINESS_BACKEND_SERVICE.md` (backend-service code)
- `PRODUCTION_READINESS_BACKOFFICE_APP.md` (backoffice-app code)
```

---

## 5. CODE_QUALITY_ASSESSMENT.md

### Purpose
Cross-cutting code quality patterns and best practices applicable to all repositories.

### Outline

```
# Code Quality Assessment - OneClick Adventures

## Executive Summary
- Multi-repository code quality analysis
- Tech Stack: TypeScript, React, Node.js, MongoDB
- Current Quality Score: X/100
- Focus: Application code only (no deployment/infrastructure)

## Scope
This document covers **code-level production readiness** across all application repositories:
- backend-service (Node.js/TypeScript API)
- agency-app (React SPA)
- backoffice-app (React SPA)

**Out of Scope:** Infrastructure, Kubernetes, deployment, and operations (see `PRODUCTION_READINESS_ALIBABA_INFRA.md`)

---

## PART 1: BACKEND SERVICE CODE QUALITY

### 1. ERROR HANDLING PATTERNS

#### 1.1 Custom Error Hierarchy
- Current: Basic Error subclasses
- Required: Comprehensive error hierarchy
- Score: 50/100
- Implementation:
  ```typescript
  // AppError base class
  // Domain-specific errors
  // HTTP status code mapping
  // Error serialization
  ```

#### 1.2 Error Middleware
- Current: Basic Express error handler
- Required: Comprehensive error handling middleware
- Implementation:
  ```typescript
  // Global error handler
  // Error logging
  // Client error formatting
  ```

#### 1.3 Retry & Circuit Breaker
- Current: No retry logic
- Required: Resilient external API calls
- Patterns:
  - Exponential backoff
  - Circuit breaker
  - Timeout handling
- Implementation:
  ```typescript
  // Retry wrapper function
  // Circuit breaker class
  ```

### 2. TYPE SAFETY & VALIDATION

#### 2.1 TypeScript Strict Mode
- Current: Partial strict mode
- Required: Full strict configuration
- Score: 70/100
- Configuration:
  ```json
  {
    "compilerOptions": {
      "strict": true,
      "strictNullChecks": true,
      "noUncheckedIndexedAccess": true,
      "exactOptionalPropertyTypes": true,
      "noImplicitReturns": true,
      "noFallthroughCasesInSwitch": true
    }
  }
  ```

#### 2.2 Zod Schema Validation
- Current: Mixed validation (some Zod, some manual)
- Required: Zod for all inputs
- Score: 60/100
- Areas:
  - Request body validation
  - Query parameter validation
  - Environment variable validation
  - External API response validation
- Implementation:
  ```typescript
  // Input schemas
  // Validation middleware
  // Type inference from schemas
  ```

#### 2.3 Branded Types
- Current: Primitive types for IDs
- Required: Branded types for type safety
- Implementation:
  ```typescript
  // Branded type definitions
  // Type guards
  // Usage in domain models
  ```

#### 2.4 Null Safety
- Required: Explicit null handling
- Patterns:
  - Optional chaining
  - Nullish coalescing
  - Type guards
  - Result types (Either pattern)

### 3. TESTING PATTERNS

#### 3.1 Unit Testing
- Current Coverage: ~10%
- Required Coverage: >70%
- Score: 30/100
- Focus Areas:
  - Domain model business logic
  - Service layer methods
  - Utility functions
  - Validators
- Framework: Jest
- Examples:
  ```typescript
  // Domain model tests
  // Service tests with mocks
  // Validator tests
  ```

#### 3.2 Integration Testing
- Current: Minimal
- Required: Comprehensive API tests
- Tools:
  - Supertest for HTTP testing
  - In-memory MongoDB (or test DB)
- Examples:
  ```typescript
  // API endpoint tests
  // Database integration tests
  ```

#### 3.3 External Service Mocking
- Current: No mocking strategy
- Required: Mock all external dependencies
- Tools:
  - nock for HTTP mocking
  - Mock repositories
- Implementation:
  ```typescript
  // Vendor API mocks
  // Payment gateway mocks
  ```

#### 3.4 Test Organization
- Required: Clear test structure
- Structure:
  ```
  tests/
  ├── unit/
  │   ├── domain/
  │   ├── services/
  │   └── utils/
  ├── integration/
  │   ├── api/
  │   └── database/
  └── e2e/
  ```

### 4. CODE ORGANIZATION & ARCHITECTURE

#### 4.1 Domain-Driven Design
- Current: Partial DDD
- Required: Complete domain layer separation
- Score: 75/100
- Layers:
  - Domain (entities, value objects, domain services)
  - Application (use cases, application services)
  - Infrastructure (repositories, external services)
  - Interface (controllers, DTOs)
- Examples:
  ```typescript
  // Domain entities with business logic
  // Value objects
  // Domain services
  ```

#### 4.2 Repository Pattern
- Current: Direct Mongoose calls in services
- Required: Repository abstraction
- Benefits:
  - Testability (mock repositories)
  - Database independence
  - Centralized query logic
- Implementation:
  ```typescript
  // Repository interfaces
  // Mongoose implementations
  // In-memory implementations for testing
  ```

#### 4.3 Service Layer
- Current: Mixed business logic in controllers and services
- Required: Thin controllers, fat services
- Pattern:
  - Controllers: HTTP handling only
  - Services: Business logic
  - Repositories: Data access
- Examples:
  ```typescript
  // Service layer examples
  // Controller delegation
  ```

#### 4.4 Dependency Injection
- Current: Manual dependency management
- Required: IoC container (InversifyJS)
- Score: 40/100
- Benefits:
  - Testability
  - Loose coupling
  - Explicit dependencies
- Implementation:
  ```typescript
  // Container setup
  // Service registration
  // Constructor injection in controllers/services
  ```

### 5. CONFIGURATION MANAGEMENT

#### 5.1 Centralized Configuration
- Current: Scattered config across files
- Required: Single source of truth
- Score: 40/100
- Implementation:
  ```typescript
  // Config service
  // Environment-based configs
  // Type-safe config access
  ```

#### 5.2 Environment Variable Validation
- Current: No validation
- Required: Zod schema validation at startup
- Implementation:
  ```typescript
  // Environment schema
  // Validation on app start
  // Type inference
  ```

#### 5.3 Feature Flags
- Current: None
- Required: Runtime feature toggling
- Use Cases:
  - Gradual rollout
  - A/B testing
  - Emergency kill switch
- Implementation:
  ```typescript
  // Feature flag service
  // Configuration source (DB or config file)
  ```

### 6. LOGGING PATTERNS

#### 6.1 Structured Logging
- Current: console.log statements
- Required: Winston with JSON format
- Score: 35/100
- Configuration:
  - Log levels (error, warn, info, debug)
  - JSON format for parsing
  - Contextual information
- Implementation:
  ```typescript
  // Logger setup
  // Log context injection
  // Usage patterns
  ```

#### 6.2 Correlation IDs
- Current: No request tracing
- Required: Correlation ID for all requests
- Benefits:
  - Trace requests across services
  - Debug distributed systems
- Implementation:
  ```typescript
  // Correlation ID middleware
  // Logger integration
  // Response header
  ```

#### 6.3 Sensitive Data Redaction
- Required: Never log passwords, tokens, credit cards
- Implementation:
  ```typescript
  // Sanitization utility
  // Logger integration
  ```

### 7. SECURITY PATTERNS (CODE LEVEL)

#### 7.1 Input Sanitization
- Required: Prevent injection attacks
- Areas:
  - NoSQL injection prevention
  - XSS prevention
  - Path traversal prevention
- Implementation:
  ```typescript
  // Sanitization utilities
  // Mongoose query sanitization
  ```

#### 7.2 Authentication & Authorization
- Current: Basic JWT
- Required: Enhanced JWT security
- Improvements:
  - RS256 signing (not HS256)
  - Token rotation
  - Refresh tokens
  - Short expiry times
- Implementation:
  ```typescript
  // JWT service with RS256
  // Token refresh logic
  // Role-based middleware
  ```

#### 7.3 Rate Limiting (Application Logic)
- Required: Protect against abuse
- Implementation:
  ```typescript
  // Express rate limiter with Redis
  // Per-route limits
  // IP-based limiting
  ```

#### 7.4 CORS Policy
- Current: Permissive CORS
- Required: Strict whitelist
- Configuration:
  ```typescript
  // CORS middleware with origin whitelist
  // Credential handling
  ```

### 8. DATABASE PATTERNS

#### 8.1 Mongoose Schema Best Practices
- Current: Basic schemas
- Required: Production-grade schemas
- Improvements:
  - Proper validation
  - Indexes for performance
  - Timestamps
  - Soft deletes
  - Schema versioning
- Examples:
  ```typescript
  // Schema with validation and indexes
  // Soft delete plugin
  ```

#### 8.2 Query Optimization
- Required: Efficient database queries
- Techniques:
  - Use indexes
  - Projection (select specific fields)
  - Lean queries
  - Aggregation pipeline
- Examples:
  ```typescript
  // Optimized queries
  // Index usage
  ```

#### 8.3 Transaction Support
- Required: ACID guarantees for multi-document operations
- Use Cases:
  - Booking + Payment creation
  - Multi-step updates
- Implementation:
  ```typescript
  // Transaction wrapper
  // Usage in services
  ```

### 9. VENDOR INTEGRATION PATTERNS

#### 9.1 Adapter Pattern Consistency
- Current: Three vendor adapters (Dingus, Hotetec, Roibos)
- Required: Consistent interface
- Review:
  - Interface adherence
  - Error normalization
  - Response mapping
- Code Review:
  ```typescript
  // IHotelVendor interface
  // Adapter implementations
  ```

#### 9.2 External API Resilience
- Required: Handle vendor failures gracefully
- Patterns:
  - Retry with exponential backoff
  - Circuit breaker
  - Timeout handling
  - Fallback responses
- Implementation:
  ```typescript
  // Resilient API client
  // Circuit breaker integration
  ```

#### 9.3 Response Caching
- Required: Cache vendor responses
- Strategy:
  - Cache hotel search results (5 minutes)
  - Cache hotel details (1 hour)
  - Redis for cache storage
- Implementation:
  ```typescript
  // Cache middleware
  // Redis integration
  ```

### 10. PAYMENT INTEGRATION PATTERNS

#### 10.1 TropiPay Service Review
- Current: Basic implementation
- Required: Production-ready
- Improvements:
  - Comprehensive error handling
  - Idempotency for payment requests
  - Webhook signature validation
- Code Review:
  ```typescript
  // TropiPay service analysis
  // Error handling improvements
  ```

#### 10.2 Webhook Security
- Required: Validate webhook signatures
- Implementation:
  ```typescript
  // Signature verification
  // Replay attack prevention
  ```

#### 10.3 Payment State Machine
- Required: Clear payment flow
- States:
  - PENDING
  - PROCESSING
  - COMPLETED
  - FAILED
  - REFUNDED
- Implementation:
  ```typescript
  // Payment status enum
  // State transition validation
  ```

---

## PART 2: FRONTEND CODE QUALITY (Agency App & Backoffice App)

### 11. REACT COMPONENT PATTERNS

#### 11.1 Container/Presentational Separation
- Current: Mixed components
- Required: Clear separation
- Score: 65/100
- Pattern:
  - Container components: Logic, state, data fetching
  - Presentational components: Pure UI, props only
- Examples:
  ```typescript
  // Container component
  // Presentational component
  ```

#### 11.2 Custom Hooks
- Current: Some custom hooks
- Required: Extract reusable logic
- Use Cases:
  - API calls (useHotelSearch)
  - Form handling (useForm)
  - Authentication (useAuth)
- Examples:
  ```typescript
  // Custom hook examples
  ```

#### 11.3 Component Composition
- Required: Composable, reusable components
- Patterns:
  - Compound components
  - Render props
  - Higher-order components (sparingly)
- Examples:
  ```typescript
  // Compound component example
  ```

### 12. STATE MANAGEMENT

#### 12.1 Redux Toolkit Best Practices
- Current: Redux Toolkit used
- Required: Optimal usage
- Patterns:
  - Normalized state shape
  - RTK Query for server state
  - Slice per feature
  - Proper typing
- Examples:
  ```typescript
  // Normalized state
  // RTK Query API slice
  ```

#### 12.2 Local vs Global State
- Required: Use local state when possible
- Guidelines:
  - Global: Authentication, user preferences, shared data
  - Local: Form state, UI toggles
- Examples:
  ```typescript
  // useState for local state
  // useSelector for global state
  ```

### 13. FRONTEND TESTING

#### 13.1 Component Unit Tests
- Current Coverage: ~5%
- Required Coverage: >70%
- Score: 20/100
- Framework: @testing-library/react
- Focus:
  - Component rendering
  - User interactions
  - Conditional rendering
  - Accessibility
- Examples:
  ```typescript
  // Component test examples
  // User interaction tests
  ```

#### 13.2 API Mocking with MSW
- Current: No mocking
- Required: Mock Service Worker for API calls
- Benefits:
  - Test in isolation
  - Reliable tests
- Setup:
  ```typescript
  // MSW handlers
  // Test setup with MSW
  ```

#### 13.3 Accessibility Testing
- Required: a11y compliance
- Tools:
  - jest-axe for automated checks
  - Manual keyboard navigation tests
- Examples:
  ```typescript
  // Accessibility test
  ```

### 14. FRONTEND TYPE SAFETY

#### 14.1 TypeScript Strict Mode
- Current: TypeScript 5.3
- Required: Strict mode enabled
- Score: 60/100
- Configuration:
  ```json
  {
    "compilerOptions": {
      "strict": true,
      "strictNullChecks": true,
      "noUncheckedIndexedAccess": true
    }
  }
  ```

#### 14.2 API Type Generation
- Current: Manual type definitions
- Required: Generate from OpenAPI spec
- Tools:
  - openapi-typescript
  - Type-safe API client
- Implementation:
  ```typescript
  // Generated API types
  // Type-safe fetch wrapper
  ```

#### 14.3 Form Validation with Zod
- Current: Basic validation
- Required: Zod schema validation
- Benefits:
  - Type inference
  - Runtime validation
  - Reusable schemas
- Examples:
  ```typescript
  // Zod form schema
  // Form component with Zod
  ```

### 15. FRONTEND SECURITY

#### 15.1 XSS Prevention
- Required: Sanitize user input
- Tools:
  - DOMPurify for HTML sanitization
  - React's built-in escaping
- Implementation:
  ```typescript
  // DOMPurify usage
  ```

#### 15.2 Authentication Token Storage
- Current: localStorage (XSS vulnerable)
- Required: httpOnly cookies
- Migration:
  ```typescript
  // Cookie-based auth
  // API changes for cookie support
  ```

#### 15.3 Content Security Policy
- Required: Strict CSP headers
- Configuration:
  ```typescript
  // Vite CSP plugin
  // CSP header configuration
  ```

### 16. FRONTEND PERFORMANCE

#### 16.1 Code Splitting
- Current: Minimal
- Required: Route-based and component-based splitting
- Tools:
  - React.lazy()
  - Suspense
- Examples:
  ```typescript
  // Route-based code splitting
  // Component lazy loading
  ```

#### 16.2 Bundle Optimization
- Required: Minimize bundle size
- Techniques:
  - Remove unused dependencies
  - Tree-shaking
  - Bundle analysis (rollup-plugin-visualizer)
- Target:
  - Agency app: <500KB initial bundle
  - Backoffice app: <600KB initial bundle

#### 16.3 Image Optimization
- Required: Optimize images
- Techniques:
  - Lazy loading
  - Responsive images (srcset)
  - WebP format
- Examples:
  ```typescript
  // Lazy image component
  ```

#### 16.4 Memoization
- Required: Prevent unnecessary re-renders
- Tools:
  - React.memo
  - useMemo
  - useCallback
- Examples:
  ```typescript
  // Memoization examples
  ```

### 17. FRONTEND LOGGING & ERROR TRACKING

#### 17.1 Client-Side Logging
- Required: Structured logging
- Implementation:
  ```typescript
  // Logger utility
  // Log levels
  ```

#### 17.2 Error Boundaries
- Current: None
- Required: Application-level error handling
- Implementation:
  ```typescript
  // ErrorBoundary component
  // Fallback UI
  ```

#### 17.3 Error Tracking Integration
- Required: Sentry or equivalent
- Configuration:
  ```typescript
  // Sentry.init()
  // Source map upload
  ```

---

## PART 3: CROSS-CUTTING CONCERNS

### 18. CODE STYLE & CONSISTENCY

#### 18.1 Linting
- Current: ESLint configured
- Required: Strict linting rules
- Configuration:
  ```json
  // .eslintrc.json
  // Recommended rules
  ```

#### 18.2 Formatting
- Current: Prettier configured
- Required: Enforce formatting in CI
- Configuration:
  ```json
  // .prettierrc
  // Format on commit (Husky + lint-staged)
  ```

#### 18.3 Naming Conventions
- Required: Consistent naming
- Conventions:
  - camelCase for variables, functions
  - PascalCase for classes, components
  - UPPER_CASE for constants
  - Descriptive names (no abbreviations)

### 19. DOCUMENTATION

#### 19.1 Code Comments
- Required: Comment complex logic
- Guidelines:
  - Why, not what
  - JSDoc for public APIs
  - No obvious comments

#### 19.2 README Files
- Required: Per-repository README
- Contents:
  - Project description
  - Setup instructions
  - Development workflow
  - Testing instructions
  - Deployment process

#### 19.3 API Documentation
- Required: OpenAPI/Swagger spec
- Tools:
  - @nestjs/swagger OR tsoa for generation
- Hosting: Swagger UI

### 20. DEPENDENCY MANAGEMENT

#### 20.1 Dependency Auditing
- Required: Regular security audits
- Tools:
  - npm audit
  - Snyk
  - Dependabot
- Frequency: Weekly

#### 20.2 Version Pinning
- Required: Lock file committed
- package-lock.json (npm) or yarn.lock (yarn)
- Avoid: Ranges in package.json for critical deps

#### 20.3 Deprecated Dependencies
- Required: Remove deprecated packages
- Process:
  - Check deprecation status monthly
  - Migrate to maintained alternatives

---

## IMPLEMENTATION PRIORITY

### P0 - Critical (Weeks 1-2)
- [ ] Enable TypeScript strict mode (all repos)
- [ ] Implement Zod validation for all inputs (backend)
- [ ] Set up error hierarchy and global error handler (backend)
- [ ] Migrate to httpOnly cookies (frontend)
- [ ] Add correlation IDs (backend)

### P1 - High (Weeks 3-4)
- [ ] Achieve >70% test coverage (backend)
- [ ] Implement dependency injection (backend)
- [ ] Set up MSW for frontend tests
- [ ] Add error boundaries (frontend)
- [ ] Configure Winston structured logging (backend)

### P2 - Medium (Weeks 5-6)
- [ ] Implement circuit breaker for external APIs (backend)
- [ ] Optimize database queries with indexes (backend)
- [ ] Implement code splitting (frontend)
- [ ] Add feature flags (backend)
- [ ] Integrate Sentry (all apps)

### P3 - Enhancement (Weeks 7-8)
- [ ] Complete DDD refactor (backend)
- [ ] Optimize bundle sizes (frontend)
- [ ] Add API documentation with Swagger
- [ ] Implement transaction support (backend)
- [ ] Add accessibility tests (frontend)

---

## SUCCESS METRICS

### Backend Service
| Metric | Current | Target |
|--------|---------|--------|
| Test Coverage | ~10% | >70% |
| TypeScript Strict | Partial | Full |
| Error Handling | Basic | Comprehensive hierarchy |
| Logging | console.log | Winston JSON |
| DI Container | Manual | InversifyJS |
| API Docs | None | OpenAPI/Swagger |

### Frontend Applications
| Metric | Current | Target |
|--------|---------|--------|
| Test Coverage | ~5% | >70% |
| TypeScript Strict | Partial | Full |
| Bundle Size | Unknown | <500KB (agency), <600KB (backoffice) |
| Error Tracking | None | Sentry |
| Error Boundaries | None | Implemented |
| Code Splitting | Minimal | Route + component level |

---

## CONCLUSION

This document provides code-level production readiness improvements for all application repositories. All infrastructure and deployment concerns are addressed in `PRODUCTION_READINESS_ALIBABA_INFRA.md`.

**Key Recommendations:**
1. **Type Safety First**: Enable strict TypeScript and Zod validation
2. **Test Coverage**: Achieve >70% coverage with unit + integration tests
3. **Error Handling**: Implement comprehensive error hierarchy and global handlers
4. **Security**: Migrate to httpOnly cookies, add input sanitization
5. **Observability**: Structured logging, correlation IDs, error tracking

---

**Document Navigation:**
- Infrastructure/Operations: `PRODUCTION_READINESS_ALIBABA_INFRA.md`
- Agency App Code: `PRODUCTION_READINESS_AGENCY_APP.md`
- Backend Service Code: `PRODUCTION_READINESS_BACKEND_SERVICE.md`
- Backoffice App Code: `PRODUCTION_READINESS_BACKOFFICE_APP.md`
```

---

## Summary

This master outline document provides the complete structure for all 5 production readiness documents **after reorganization**:

1. **Application Documents** (agency-app, backend-service, backoffice-app):
   - Focus: Code quality, testing, architecture, application-level security
   - Exclude: Kubernetes, infrastructure, deployment, operations

2. **Infrastructure Document** (alibaba-infra):
   - Focus: All deployment, K8s, infrastructure automation, networking, databases
   - Includes: Application deployment manifests, HPA, health checks, network policies

3. **Code Quality Document**:
   - Focus: Cross-cutting code patterns applicable to all applications
   - Exclude: Infrastructure and deployment concerns

This separation ensures each document has a single, cohesive purpose for engineering teams.