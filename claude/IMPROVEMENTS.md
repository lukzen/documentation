# Claude Code setup — improvement backlog

Reviewed 2026-08-31 against [clean-code-skill](https://github.com/unrealandychan/clean-code-skill), [swarm-forge](https://github.com/unclebob/swarm-forge) and [claude-howto](https://github.com/luongnv89/claude-howto). Baseline = the shared setup in this folder (installed via `install.sh`) plus the per-repo CLAUDE.md / copilot-instructions already in git.

| # | Improvement | Source | Where | Effort | Pick | Why |
|---|-------------|--------|-------|--------|------|-----|
| 1 | ESLint DDD boundary rules — layering (route→service→repo→model) + cross-domain imports only via `public.ts` | clean-code-skill | backend-service | S | **DO this week** | The two most load-bearing backend rules exist only as prose; enforcement is "a reviewer notices". A direct import of a credit model compiles fine today and silently breaks the seam F1's payment design depends on. A lint rule turns that into a CI failure — and this week adds `/internal/saga/*`, exactly the kind of fast-written new surface that violates layering. |
| 2 | PR template with the agent-pipeline checklist (code-simplifier → review-robustness + review-testquality → 1 Copilot round) and an "implements spec § / AC __" line | swarm-forge + clean-code-skill | `.github/PULL_REQUEST_TEMPLATE.md` in backend-service, agency-app, backoffice-app | S | **DO this week** | A README is advice; a PR template is a gate every PR walks through — and how teammates discover the process without being told. The AC-citation line is what made the 08-31 ticket-vs-spec drift findable; without it PRs drift from spec silently. |
| 3 | Correlation-id log rule: `sagaExecutionId` + `idempotencyKey` on every `/internal/saga/*` log line | clean-code-skill (observability) | backend `.github/copilot-instructions.md` | XS | **DO with day-2 routes** | Spec §3b's troubleshooting story is "one jump from a booking to its trace" — broken if any log line lacks the ids. One sentence now vs an afternoon of retrofitting after the first incident. |
| 4 | Team slash commands: `/publish-docs`, `/pr-ready`, `/verify-local` | claude-howto §01 | this folder + repo `.claude/commands/` | M | DO after build week | The portal publish chain alone is a 6-step runbook executed from memory ~10×/day — it mis-ordered twice before the sequence settled. A slash command makes the runbook executable: a teammate's first publish behaves like the hundredth, and process fixes ship as `git pull`, not a retelling. |
| 5 | Directory-scoped CLAUDE.md: `domains/backoffice/` (seam rule), `cypress/` (tag taxonomy) | claude-howto §02 | backend-service, agency-app | S | DO opportunistically | Rules bind best where the violation happens — in-context when the file opens, not diluted at repo root. The cypress tag taxonomy earned its own runbook precisely because misuse kept recurring. |
| 6 | Starter personal `~/.claude/CLAUDE.md` template for teammates | claude-howto §02 | this folder | XS | Optional | Onboarding nicety: sane defaults (decision format, verify habits) instead of an empty file. Nothing breaks without it. |
| 7 | Headless `claude -p` weekly doc-accuracy audit in CI | claude-howto §09 | technical-docs CI | M | Backlog (post-GKE) | The 08-30 manual audit of 37 docs caught leaked credentials and a false "merged" claim — drift accumulates silently, the sweep pays for itself. But it automates a monthly-cadence task during a hard-deadline week, and the CI runner story is cleaner on GKE. Right idea, wrong week. |
| 8 | Shared MCP configs (`.mcp.json`) | claude-howto §05 | repos | S | Skip for now | Everything MCP would give us for GitHub, `gh` CLI already does — authenticated, scriptable, in daily use. A second path to the same API is a new credential surface with zero new capability. Revisit when a needed tool has no CLI. |
| 9 | swarm-forge as a platform (tmux + handoff daemon + dashboard) | swarm-forge | — | L | **Skip** | Solves coordination for large agent fleets across big teams; at 2–3 people the Agent tool already provides roles, worktree isolation and background handoffs with zero standing infrastructure. Same test that ruled out Temporal in spec §1b: machinery without payload at this scale. Its role-pipeline idea is kept — as row 2. |
| 10 | clean-code-skill wizard install (its CLAUDE.md adapter) | clean-code-skill | — | S | **Skip** | Would install a second overlapping ruleset next to the Karpathy hook and house instructions — conflicting rule sources make the model silently average them (Karpathy rule 7). Cherry-picking its best rules (rows 1, 3) gets the value without a second source of truth. |

**Already absorbed into the shared setup (2026-08-31, no row needed):** spec-§/AC-citation before any agent work, the role pipeline as a written working agreement, plugin-set sync via `install.sh`, Gherkin ACs on the active ticket.

**Execution order:** 1 → 2 → 3 inside the F1 week; 4 → 5 right after; 6–7 as capacity allows.
